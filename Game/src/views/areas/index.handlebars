<table class="table table-hover">
  <thead>
    <tr>
      <th >Áreas da Ciência</th>
    </tr>
  </thead>
  <tbody>
    {{#each areas}}
      <tr class="table-primary">
        <th scope="row">{{name}}</th>
      </tr>
    {{/each}}
  </tbody>
</table>